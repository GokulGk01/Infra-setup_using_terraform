version: '3.9'
services:
    frontend:
        build: /var/www/{{project_name}}/frontend
        restart: always
        network_mode: {{ docker_network_name }}
        ports:
            - '8008:80'
        container_name: frontend
        image: frontend
