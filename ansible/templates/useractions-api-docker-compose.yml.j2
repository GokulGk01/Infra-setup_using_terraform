version: '3.9'
services:
    useractions-api:
        build: /var/www/{{project_name}}/useractions-api
        restart: always
        container_name: useractions
        network_mode: {{ docker_network_name }}
        image: useractions-api
        volumes:
          - ../env:/usr/src/app/.env
